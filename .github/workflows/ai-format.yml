name: AI Format Check

on:
  pull_request:
    paths:
      - "ai/**"

jobs:
  ai-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Dummy formatter (placeholder)
        run: echo "AI format check passed!"
