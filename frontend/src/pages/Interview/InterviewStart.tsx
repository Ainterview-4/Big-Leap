import React from "react";
import { Container, Typography } from "@mui/material";

const InterviewStart: React.FC = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Interview Start Page</Typography>
    </Container>
  );
};

export default InterviewStart;
