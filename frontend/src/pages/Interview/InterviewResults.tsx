import React from "react";
import { Container, Typography } from "@mui/material";

const InterviewResults: React.FC = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Interview Results Page</Typography>
    </Container>
  );
};

export default InterviewResults;
