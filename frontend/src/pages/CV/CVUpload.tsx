import React from "react";
import { Container, Typography } from "@mui/material";

const CVUpload: React.FC = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">CV Upload Page</Typography>
    </Container>
  );
};

export default CVUpload;
