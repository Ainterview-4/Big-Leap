import React from "react";
import { Container, Typography } from "@mui/material";

const CVResult: React.FC = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">CV Result Page</Typography>
    </Container>
  );
};

export default CVResult;
