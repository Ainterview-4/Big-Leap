# ===========================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# ===========================================

# Server Configuration
NODE_ENV=development
PORT=5001

# CORS - Comma-separated list of allowed origins
# For local development, both common Vite ports are allowed
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Database (PostgreSQL)
# Format: postgresql://username:password@host:port/database?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/bigleap?schema=public

# Security
# IMPORTANT: Use a strong secret (min 32 characters) in production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# S3/Cloudflare R2 (Optional for local development)
# Leave empty for local dev, required for production
S3_REGION=us-east-1
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE_URL=